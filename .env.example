# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Premium reasoning model — narrative synthesis (the research note prose)
ANTHROPIC_API_KEY=

# Fast, low-cost model — filing extraction / structuring
GEMINI_API_KEY=
